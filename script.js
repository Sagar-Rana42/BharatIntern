const menu = document.querySelector('#menu');

const navvar = document.querySelector('.navbar');

menu.addEventListener(onClick,()=>{
    
})
menuIcon.onClick = ()=>{
    
    menuIcon.classList.toggle('bx-x');
    navvar.classList.toggle('active');
}